// import { flush, render } from '@stencil/core/testing';
import { SlimSlider } from './slimslider';

describe('slimslider', () => {
  it('should build', () => {
    expect(new SlimSlider()).toBeTruthy();
  });
});
